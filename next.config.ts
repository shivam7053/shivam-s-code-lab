import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // If deploying to GitHub Pages, uncomment the lines below:
  // output: "export",
  // images: {
  //   unoptimized: true,
  // },
  // basePath: "/shivams-code-lab", // Replace with your repo name
};

export default nextConfig;